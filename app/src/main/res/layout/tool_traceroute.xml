<?xml version="1.0" encoding="utf-8"?>
<!-- Layout para la herramienta Traceroute -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#0D1117">

    <!-- Fixed header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp"
        android:gravity="center_horizontal">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/traceroute_title"
            android:textColor="#FF00E5FF"
            android:textSize="22sp"
            android:textStyle="bold"
            android:fontFamily="sans-serif-medium"
            android:layout_marginBottom="8dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Traza la ruta de paquetes hasta un destino"
            android:textColor="#FF8B949E"
            android:textSize="13sp"
            android:layout_marginBottom="20dp" />

        <!-- Host input -->
        <EditText
            android:id="@+id/editTraceHost"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/traceroute_host_hint"
            android:textColor="#FFE6EDF3"
            android:textColorHint="#FF6E7681"
            android:textSize="15sp"
            android:fontFamily="monospace"
            android:background="@drawable/card_bg_about"
            android:padding="14dp"
            android:singleLine="true"
            android:inputType="textUri"
            android:layout_marginBottom="10dp" />

        <!-- Max hops + start row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginBottom="14dp">

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical"
                android:layout_marginEnd="10dp">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/traceroute_max_hops"
                    android:textColor="#FF6E7681"
                    android:textSize="11sp"
                    android:fontFamily="sans-serif-medium"
                    android:letterSpacing="0.06"
                    android:layout_marginBottom="4dp" />

                <EditText
                    android:id="@+id/editMaxHops"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="30"
                    android:textColor="#FFE6EDF3"
                    android:textSize="14sp"
                    android:fontFamily="monospace"
                    android:background="@drawable/card_bg_about"
                    android:padding="12dp"
                    android:singleLine="true"
                    android:inputType="number"
                    android:gravity="center" />
            </LinearLayout>

            <TextView
                android:id="@+id/btnStartTrace"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/traceroute_start"
                android:textColor="#FF0D1117"
                android:textSize="15sp"
                android:textStyle="bold"
                android:fontFamily="sans-serif-medium"
                android:gravity="center"
                android:padding="14dp"
                android:background="@drawable/badge_online"
                android:clickable="true"
                android:focusable="true" />
        </LinearLayout>

        <!-- Progress -->
        <ProgressBar
            android:id="@+id/traceProgress"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="3dp"
            android:visibility="gone"
            android:progressTint="#FF00E5FF"
            android:progressBackgroundTint="#FF21262D"
            android:layout_marginBottom="6dp" />

        <!-- Done indicator -->
        <TextView
            android:id="@+id/txtTraceDone"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#FF3FB950"
            android:textSize="13sp"
            android:visibility="gone" />
    </LinearLayout>

    <!-- Results list (scrollable) -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingHorizontal="24dp">

        <LinearLayout
            android:id="@+id/traceResultsList"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="24dp" />
    </ScrollView>

</LinearLayout>
