Nota de compatibilidad del proyecto:

El manifiesto utilizado por Gradle es:
app/src/main/AndroidManifest.xml

El archivo AndroidManifest.xml ubicado en la raíz es un artefacto legado y no participa en el build actual de la aplicación.
